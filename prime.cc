#include <iosteam>
#include <stdlib.h>
#include <math.h>

struct primes {
	int prime;
}

int next_divident (int prime, int progress) {
	do {
		if (prime%progress==0) {primes[progress]=1; return progress;}
	} while(progress<=sqrt(prime);
	return 0;
}

void potenz (int prime, int divident, int divident count) {
	int i=2;
	do {
		if (prime%(i*divident)==0) {potenz[count]++;}
		else {i=prime;)
		i++;
	} while (i<=sqrt(prime))
}

int main (int argc, char** argv) {
	do while 
	 	
